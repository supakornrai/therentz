import 'package:flutter/material.dart';

class StaffInboxView extends StatelessWidget{
  const StaffInboxView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
body: Center(
  child: Text("StaffInboxView"),
),

    );
    
  }

}